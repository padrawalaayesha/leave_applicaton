<!DOCTYPE html>
<html>
<head>
  <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  <style>
    /* Global Styles */
    body {
      font-family: 'Arial', sans-serif;
      background-color: #f9f9f9;
      margin: 0;
      padding: 0;
      color: #333333;
    }

    /* Container Styles */
    .container {
      max-width: 600px;
      margin: 20px auto;
      padding: 20px;
      background-color: #ffffff;
      border-radius: 8px;
      box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
    }

    /* Heading Styles */
    h1 {
      color: #007bff; /* Blue color for emphasis */
      text-align: center;
      margin-top: 0;
      margin-bottom: 20px; /* Added spacing below heading */
    }

    /* Paragraph Styles */
    p {
      margin-bottom: 10px;
      line-height: 1.6;
    }

    /* Status Styles */
    .status {
      font-weight: bold;
      color: #28a745; /* Green color for positive status */
    }

    /* Signature Styles */
    .signature {
      margin-top: 20px;
      font-style: italic;
      text-align: center;
    }

    /* Responsive Styles */
    @media screen and (max-width: 600px) {
      .container {
        width: 90%;
        margin: 10px auto;
        padding: 10px;
      }
    }
    
    /* Additional Styles */
    /* Highlighted Background for Paragraphs */
    .highlight {
      background-color: #f0f8ff; /* Light blue background */
      padding: 10px;
      border-radius: 8px;
    }
    
    /* Rounded Corners for Status Span */
    .status {
      padding: 5px 10px;
      border-radius: 5px;
      background-color: #e6ffe6; /* Light green background */
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Leave Status Notification</h1>
    <p>Dear <%= @employee.name %>,</p>
    <% if @holiday.present? %>
      <div class="highlight"> <!-- Added highlight class -->
        <p>Regarding your leave request: <%= @holiday.description %></p>
        <p>From: <%= @holiday.start_date %>&nbsp; To: <%= @holiday.end_date %></p>
        <p>Number of days: <%=(@holiday.end_date - @holiday.start_date).to_i + 1%></p>
      </div>
    <% else %>
      <p>Regarding your leave request: [No holiday information]</p>
    <% end %>
    <p>Status: <span class="status"><%= @message %></span></p> <!-- Added rounded corners for status span -->
    <p>Thank you.</p>
    <div class="signature">
      <p>Best regards,<br>
      Soft Coding Solutions</p>
    </div>
  </div>
</body>
</html>
